#include <iostream>
#include <numbers>

#include "../cylinder.hpp"

int main() {
    bool error = false;

    geometry::Cylinder cylinder({0, 0}, {1, 1}, 2);

    if (std::abs(cylinder.baseSquare() - )

    return error;
}
